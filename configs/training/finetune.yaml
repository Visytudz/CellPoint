# hyperparameters
batch_size: 8
epochs: 300
warmup_epochs: 10
lr: 0.0005
weight_decay: 0.05
min_lr: 1e-6

# other settings
num_workers: 4
val_interval: 10
freeze_encoder: true
checkpoint_path: null
resume_training: false